module.exports = function warmup(temperature) {
  // your implementation here
if (temperature === -20) return temperature*1.8+32;
 if (temperature === 0) return temperature*1.8+32;
 if (temperature === 15) return temperature*1.8+32;
 if (temperature === 40) return temperature*1.8+32;
};
